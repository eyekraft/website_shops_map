<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- "Period of time" form view -->
    <record id="period_of_time_view_form" model="ir.ui.view">
        <field name="name">period.of.time.form</field>
        <field name="model">period.of.time</field>
        <field name="arch" type="xml">
            <form string="Period of time">
                <sheet>
                    <group>
                        <group>
                            <field name="start_time" widget="float_time"/>
                        </group>
                        <group>
                            <field name="end_time" widget="float_time"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- "Period of time" ir.actions.act_window -->
    <record id="period_of_time_action" model="ir.actions.act_window">
        <field name="name">Periods of Time</field>
        <field name="res_model">period.of.time</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a Periods of Time.
            </p>
            <p style="text-align: left">
                Create a Periods of Time entering opening hours for the shop
                <ul style="text-align: left">
                    <li><i>Since</i>, defines the hour from in which the shop works, in set hours in numbers</li>
                    <li><i>To</i>, defines the hour until in which the shop works, in set hours in numbers</li>
                </ul>
            </p>
        </field>
    </record>

    <!-- "Period of time" menu item -->
    <menuitem id="period_of_time_menu"
        action="period_of_time_action"
        parent="menu_shop_config"
        name="Periods of Time"
        sequence="120" />
</odoo>
